package file_manager;

public class Trials {
    public static class createUserSchema {
/*
        Map<String, String> schema = new HashMap<>();
        schema.put("username", "String");
        schema.put("email", "String");
        schema.put("phone", "String");
        schema.put("password", "String");
        Writer.designSchema("user", schema);
        */
/*
        Map<String, String> user = new HashMap<>();
        user.put("username", "Oswald Cabolpot");
        user.put("email", "thePenguin@gmail.com");
        user.put("phone", "000000000002");
        user.put("password", "123456789");

        write("user", user);
        */
/*
        Map<Integer, Map<String, String>> user = readOne("user", "phone", "000000000001");

        for (Map.Entry<Integer, Map<String, String>> map : user.entrySet())
        {
            System.out.println(map.getKey());
            for (Map.Entry<String, String> innerMap : map.getValue().entrySet())
            {
                System.out.println("\t" + innerMap.getKey() + ": " + innerMap.getValue());
            }
        }
        */

    }
}
